# 端口占用问题解决方案

## 问题描述

错误信息：
```
Error: listen EADDRINUSE: address already in use :::8000
code: 'EADDRINUSE',
errno: -4091,
syscall: 'listen',
address: '::',
port: 8000
```

**原因**：端口 8000 已被其他进程占用，通常是之前启动的服务器进程没有正确关闭。

---

## 解决方案

### 方法1：查找并终止占用端口的进程（推荐）

#### Windows PowerShell/CMD

1. **查找占用端口的进程**：
   ```powershell
   netstat -ano | findstr :8000
   ```
   
   输出示例：
   ```
   TCP    0.0.0.0:8000           0.0.0.0:0              LISTENING       25004
   ```
   
   最后一列是进程ID（PID）：`25004`

2. **终止进程**：
   ```powershell
   taskkill /PID 25004 /F
   ```
   
   `/F` 参数表示强制终止

3. **重新启动服务器**：
   ```powershell
   npm run dev
   ```

#### 一行命令（PowerShell）

```powershell
Get-Process -Id (Get-NetTCPConnection -LocalPort 8000).OwningProcess | Stop-Process -Force
```

### 方法2：修改端口号

如果不想终止进程，可以修改服务器端口：

1. **修改 `.env` 文件**（如果存在）：
   ```
   PORT=8001
   ```

2. **或修改 `src/index.js`**：
   ```javascript
   const PORT = process.env.PORT || 8001;  // 改为8001或其他端口
   ```

### 方法3：使用不同的端口启动

```powershell
$env:PORT=8001; npm run dev
```

---

## 预防措施

### 1. 正确关闭服务器

- 使用 `Ctrl + C` 停止 nodemon
- 确保进程完全退出后再重新启动

### 2. 检查进程是否还在运行

```powershell
# 查看所有Node.js进程
Get-Process node -ErrorAction SilentlyContinue

# 查看特定端口的进程
netstat -ano | findstr :8000
```

### 3. 添加端口检查代码（可选）

可以在启动前检查端口是否可用，但通常不需要，因为错误信息已经很清楚了。

---

## 常见场景

### 场景1：多个终端窗口

如果你在多个终端窗口运行了 `npm run dev`，只有第一个会成功，其他的会报这个错误。

**解决**：关闭其他终端窗口的服务器，只保留一个。

### 场景2：进程没有正确退出

有时即使按了 `Ctrl + C`，进程可能还在后台运行。

**解决**：使用 `taskkill` 命令强制终止。

### 场景3：其他应用占用端口

可能有其他应用也在使用 8000 端口。

**解决**：
1. 查找占用端口的进程
2. 决定是终止该进程还是修改你的应用端口

---

## 快速检查脚本

创建一个 PowerShell 脚本 `check-port.ps1`：

```powershell
$port = 8000
$connections = Get-NetTCPConnection -LocalPort $port -ErrorAction SilentlyContinue

if ($connections) {
    Write-Host "Port $port is in use by:" -ForegroundColor Yellow
    foreach ($conn in $connections) {
        $process = Get-Process -Id $conn.OwningProcess -ErrorAction SilentlyContinue
        if ($process) {
            Write-Host "  PID: $($conn.OwningProcess) - $($process.ProcessName)" -ForegroundColor Red
        }
    }
    Write-Host "`nTo kill the process, run:" -ForegroundColor Yellow
    Write-Host "  taskkill /PID $($connections[0].OwningProcess) /F" -ForegroundColor Cyan
} else {
    Write-Host "Port $port is available" -ForegroundColor Green
}
```

使用方法：
```powershell
.\check-port.ps1
```

---

## 总结

**问题**：端口 8000 被占用  
**解决**：终止占用端口的进程或修改端口号  
**命令**：
```powershell
# 查找进程
netstat -ano | findstr :8000

# 终止进程（替换PID为实际进程ID）
taskkill /PID <PID> /F
```

现在你可以重新运行 `npm run dev` 了！

